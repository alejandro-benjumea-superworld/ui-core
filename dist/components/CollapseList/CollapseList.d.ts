import { ReactElement } from 'react';
import type { CollapseListProps, ListItem } from './CollapseList.types';
export declare const CollapseList: <T extends ListItem>(props: CollapseListProps<T>) => ReactElement;
